services:
  - type: web
    name: turbo-executions
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.api.server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
      - key: ACCESS_CODE
        sync: false  # Set this in Render dashboard
      - key: MT5_LOGIN
        value: 405773
      - key: MT5_PASSWORD
        value: N8odc?1bVD
      - key: MT5_SERVER
        value: EquityEdge-Trade
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 8000
      - key: MOCK_MODE
        value: "0"  # Enable live trading mode
